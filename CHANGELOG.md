# 변경 기록

## 0.19.8 — 새 채팅의 큰 클립보드 이미지

- 새 채팅 첫 입력에 클립보드 이미지가 포함되면 내부 WebSocket의 1MB 제한이 먼저 연결을 끊을 수 있던 문제 수정. 통신 상한은 12MB로 올리고 실제 이미지 합계 8MB 검증은 유지.
- 최대 크기 이미지가 실제 WebSocket 입력 경로를 통과하는 통합 검사 추가. 임의 명령 실행 차단과 연결 인증 조건은 유지. 0.19.7의 큰 대화 전송·연결 안정화 수정도 포함.

## 0.19.7 — 큰 대화 연결 안정화와 이미지 입력

- 이미지가 들어 있는 큰 작업을 변경마다 전체 전송하던 방식을 첫 상태 + 변경분 전송으로 변경. 큰 초기 응답은 압축하고 출력이 밀리면 중간 상태를 합쳐 최신 상태만 전송.
- SSH 수신 조각을 매번 처음부터 다시 검색하지 않고 한 번씩 처리. 데이터가 실제로 도착하는 동안에는 무응답으로 오판하지 않으며, 무응답 15초·미완성 프레임 120초 상한은 유지.
- 일반 텍스트와 함께 첨부한 PNG/JPEG/GIF/WebP 이미지를 최대 4개·합계 8MB까지 전달. 노트북 파일 경로를 GPU 경로로 해석하지 않고 파일 바이트를 전송하며 GPU에서 형식·크기·해시 재검증. 새 작업의 첫 입력에도 적용.
- 전송 결과가 불확실한 입력은 자동 재전송하지 않음. 스트림의 임시 메시지만으로 성공을 만들어 내지 않고 원래 GPU 소유자의 응답을 유지.
- Codex 26.915의 원격 입력 경로에서 전송 후 작성란이 남는 문제는 별도 제한 사항으로 기록. 공식 앱 바이너리나 작성란 데이터를 강제로 변경하지 않음.

## 0.19.6 — GPU에서 직접 작업한 최근 대화 복원

- GPU 공식 앱에서 직접 이어간 대화가 실제 활성 파일에는 남아 있지만 공식 최근 목록 색인에서 빠지는 경우를 복원. 제목이나 프로젝트명을 추정하지 않고, 최근 활성 대화 파일의 ID와 수정 시각을 공식 `thread/read` 결과와 대조.
- 첫 최근 목록에서 누락된 사용자 루트 대화를 복원하고, GPU 공식 앱의 `recencyAt`(최근에 본 시각)을 유지해 직접 GPU에서 보던 순서로 표시. 내용 파일의 `updatedAt`만 바뀐 가져오기·백그라운드 작업이 최근 순서를 덮어쓰지 않음. 하위 에이전트·승인 검토·임시 대화·보관 대화·읽을 수 없는 대화는 제외.
- 복원 검사는 대화 파일 이름과 수정 시각만 확인하고 본문 파일을 직접 읽거나 수정하지 않음. 복원된 항목을 열 때도 기존과 같이 공식 대화 읽기와 GPU 소유자 확인을 거쳐야 입력 가능.
- GPU 다운 뒤 남은 세션 파일, 보관 목록, 공식 활성 목록을 분리해 진단하고 특정 제목에 의존하지 않는 회귀 검사를 추가.

## 0.19.5 — 최신 작업 표시와 열기 속도 개선

- 창을 띄우기 전에 298개 전체 작업을 끝까지 읽던 절차를 제거. 작은 프로젝트 목록과 마지막 선택 작업만 확인한 뒤 화면을 열고, 최근 목록의 첫 페이지를 GPU 공식 API에서 즉시 반환하도록 변경.
- 창 시작과 작업 선택 뒤 5초 동안 사용자 요청을 우선하고, 전체 목록 재검색 간격을 10초에서 30초로 조정해 GPU의 공식 앱·CLI와 배경 읽기가 경쟁하는 시간을 줄임.
- 저장 본문 사전 읽기를 최신 32개 작업으로 제한하고, 목록 순서가 바뀌면 최신 창을 다시 구성. 미리 읽지 않은 작업을 클릭하면 해당 본문을 즉시 우선 조회해 화면을 먼저 표시하고, 실제 GPU 소유자 확인 전까지 입력은 계속 차단.
- 제한보다 큰 저장 본문은 같은 작업 버전에서 매 목록 검사마다 다시 읽지 않음. 해당 작업이 실제로 갱신될 때만 다시 판정.
- 새 최상단 작업을 발견하면 캐시된 목록을 폐기한 뒤 GPU의 최신 첫 페이지를 다시 읽는 회귀 검사 추가. 본문 사전 읽기는 작업을 열거나 지시·승인을 전송하지 않음.
- 공식 데스크톱이 일반 `최근` 목록에 보내는 빈 프로젝트 필터를 제거해, 프로젝트 폴더에서 진행 중인 작업도 최근 순서에 그대로 표시. 프로젝트를 펼쳐 조회할 때의 폴더 필터는 유지.

## 0.19.4 — 긴 대화 과거 내용 불러오기

- 실시간 상태에 최근 턴만 포함된 긴 작업에서 이전 내용 요청 `thread/turns/list`가 차단되던 누락 수정.
- 작업 ID, 커서, 페이지 크기, 정렬 방향, 항목 표시 수준을 제한한 읽기 전용 경로로만 GPU 공식 API에 전달. 목록에 없는 작업, 알 수 없는 매개변수, 중복 턴, 비정상 커서와 과대 응답은 거부.
- 과거 내용 조회는 작업을 열거나 실행하지 않으며, 메시지·승인·설정 변경을 전송하지 않음. 진단에는 커서나 본문 대신 요청 유무와 반환 개수만 기록.

## 0.19.3 — 최신 Codex 호환성과 갱신 상태 표시

- 노트북과 GPU의 Codex 26.915.4065.0, GPU 공식 CLI 0.155.0-alpha.9.2 지원 추가. 상태 스트림·입력·승인·모델 설정 통신 버전을 양쪽 설치 파일에서 확인.
- 최신 호스트의 모델 설정 v2 조건과 `applied` 결과를 그대로 전달. 이전 v1 호스트에는 조건부 변경을 무조건 쓰기로 낮추지 않음.
- GPU 연결이 끊겼을 때 화면의 본문이 마지막 수신본이며 최신 내용이 아닐 수 있음을 연결 창에 표시. 새 GPU 상태를 받으면 안내를 제거하고 최신 전체 상태로 교체.
- 앱 또는 CLI 버전 불일치를 일반 SSH 종료로 숨기지 않고, 개인정보가 섞일 수 있는 원문 진단을 노출하지 않은 채 원인을 분류해 안내.

## 0.19.2 — 대화 보관·복원 연결

- 연결 창에서 보관 버튼을 눌렀을 때 `thread/archive`가 차단되던 누락 수정. 보관과 보관 해제를 정확한 GPU 작업 ID에 한정해 공식 app-server API로 전달.
- GPU 성공 응답 이후 대화 목록·본문 캐시 갱신 및 보관 알림 전달. 보관된 대화는 이전 관찰 응답이나 재연결 유지 과정에서 자동으로 다시 열지 않음.
- 연결이 끊기거나 응답을 확인하지 못하면 보관 성공을 표시하거나 자동 재전송하지 않음. GPU에 작업 ID·요청 종류만 기록하여 중복 요청을 방지.
- 원격 파일 직접 이동, DB 수정, 작업 실행 또는 워크트리 삭제 기능은 추가하지 않음.

## 0.19.1 — 인터넷 끊김 후 대화 복구

- 통신 복구 뒤 현재 보고 있는 대화를 다시 열어 `no-client-found` 반복을 해결. 목록에만 있거나 보관 중인 다른 대화는 자동으로 열지 않음.
- 같은 GPU 소유자의 대화가 다시 로드되며 상태 번호가 낮아지는 경우, 현재 연결에서 요청한 전체 상태 응답으로만 기준을 갱신. 이전 연결 응답, 다른 대화·소유자, 온라인 상태의 역행은 계속 거부.
- 노트북 화면에 전달하는 상태 번호는 계속 증가하며, 지시·승인을 자동 재전송하지 않음.


## 0.19.0 — 승인·질문 응답 확장

- 파일 변경, 선택형/직접 입력 질문과 초기 설정 질문, 일반 MCP/URL/스칼라 확장 폼 응답 추가. 원래 GPU 요청·소유자·턴을 재검증하고 중복·불확실 응답의 재전송을 방지.
- 명령의 네트워크 정책 변경 제안과 개별 STDIN 승인 콜백 지원. 요청에 제시하지 않은 정책은 전달하지 않음.
- 원본 계획 실행 버튼을 마지막 완료 계획에 묶어 Plan → Default 모드로 전달. GPU 지침·권한 유지.
- 원격 응답 경로가 없는 옵션 선택기 응답 때문에 앱 연결이 끊기는 문제 수정. 미지원 요청에 연결 창 안내 추가.
- 질문·폼 값은 새 응답 기록에 저장하지 않고 해시만 보관. 인증/설치/파일 폼 등 남은 제한은 승인 지원표에 명시.

## 0.18.3 — 바탕 화면 설치 복구 및 권한·대기열 조회 연결

- MSIX 앱의 하위 프로세스에서 설치하면 AppData가 앱 전용 폴더로 리디렉션되어 바탕 화면 아이콘에서 보이지 않을 수 있는 원인 확인.
- 두 설치 대상 폴더에 고유 임시 파일을 써서 실제 저장 경로를 확인하고, 리디렉션되면 설치 파일·포인터·아이콘 작성 전에 거부하고, Explorer에서 Install.cmd를 실행하도록 안내.
- 프로그램 파일 검증 후에만 검사 모듈을 로드. 설치 복구 시험과 실행 환경 시험을 분리.
- `thread/queue/list` 조회 경로 누락 수정. 작업 ID와 페이지 인수를 검사하고 GPU의 최신 대기열을 읽음. 조회 실패를 빈 대기열로 처리하지 않음.
- 인터넷·파일 시스템 추가 권한 카드의 사용자 응답 전달 지원. 원래 요청의 권한 그룹과 허용 기간을 보존하고, 활성 요청·소유자를 다시 확인한 후 한 번만 전달. 요청 경로는 승인 기록에 저장하지 않음.


## 0.18.2 — Codex 업데이트 호환성

- 노트북 앱 26.908.4834.0과 호스트 앱 26.903.8094.0 조합 지원 추가. 알 수 없는 빌드는 계속 차단.
- 새 모델 설정 요청의 `applied` 응답을 이전 호스트의 확인 응답에서 변환.
- 조건부 요청을 일반 쓰기로 바꾸지 않으며 실행 중 턴의 권한 변경을 모델 선택으로 처리하지 않음.
- 두 모델 설정 규약, 잘못된 응답 및 조건부 요청의 무전송을 회귀 검사.


## 0.18.1 — 설치 복구 및 연결 안정성 개선

- 설치 파일을 임시 폴더에서 복사·검증한 뒤 완성된 버전 폴더로 적용. 중간 실패는 재실행으로 복구하고, 구형 미완성 설치는 검증 후 보존. 정상·변조·사용 중·등록된 설치는 덮어쓰지 않음.
- 설치 포인터를 백업과 함께 원자적으로 교체하고 Windows 사용자별 설치 동시 실행 방지.
- 실제 Windows 사용자 SID에 따라 연결 창 중복 실행 제한을 분리.
- 명령 승인 시각의 비정상 값·미래 값 거부. 실제 요청·선택 대조와 재전송 방지 유지.
- 진단 JSON의 원자적 교체, 읽기 잠금 중 이전 보고서 유지, 정상 종료 시 해당 실행이 만든 연결 파일 정리.
- CLI 클라이언트 식별 버전을 package.json과 일치시킴.
- OneDrive 클라우드 파일 표식을 실제 링크와 구분하고 파일 해시 검증을 유지.
- 비정상 종료 후 PID가 재사용되어도 충돌하지 않는 연결 파일명, 기존/신규 파일 형식의 창 복원 지원.
- 진단 기록을 200ms 단위로 병합하고 상태 전이는 즉시 기록. 종료 시 잠금으로 실패한 최종 기록은 제한적으로 재시도.
- 설치·실행 뮤텍스를 동일 사용자 로그인 세션 간에도 공유하고, 절단 파일의 수동 보존·복구 절차 및 실제 CIM 검사 스크립트 추가.
- 진단 폴더 삭제·디스크 부족이 실행 중 연결을 종료하지 않도록 처리하고, 강제 종료로 남은 런처 잠금 즉시 복구.
- 설치 완료 명세 복사본의 해시까지 적용 전에 검증. 클라우드/실제 링크 태그와 손상된 명세 거부 검사 추가.

## 0.18.0 — 첫 공개 시험판

- 개인 PC 전용 주소·사용자명·설치 경로를 기기별 연결 설정으로 교체.
- 사용자별 Windows 설치, 호스트/클라이언트 역할, 바탕 화면 실행 아이콘, 기존 창 복원.
- SSH 서버 지문 확인, 키/agent 인증, 연결 진단과 호스트 등록.
- CLI 캐시 자동 탐색, 역할별 Codex 호환성 검사, 양쪽 브리지 버전 일치 확인.
- 호스트별 설정·최근 작업·보고서 분리. 첫 작업을 개인 고정 ID 대신 호스트 목록에서 선택.
- Node.js 및 고정 의존성을 포함한 오프라인 ZIP, 체크섬, 업데이트·제거 기능.
- 기존 세션·프로젝트 조회, 본문 사전 읽기, 입력, 새 작업, 모델 및 검증된 승인 전달 유지.
- 개요, 사용설명서, 호환성·문제 해결·승인 지원표 추가.

기존 개인용 버전의 대화, 진단 기록, 계정 데이터와 체크포인트는 공개 저장소에 포함하지 않습니다.
