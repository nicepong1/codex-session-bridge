# 변경 기록

## 0.19.1 — 인터넷 끊김 후 대화 복구

- 통신 복구 뒤 현재 보고 있는 대화를 다시 열어 `no-client-found` 반복을 해결. 목록에만 있거나 보관 중인 다른 대화는 자동으로 열지 않음.
- 같은 GPU 소유자의 대화가 다시 로드되며 상태 번호가 낮아지는 경우, 현재 연결에서 요청한 전체 상태 응답으로만 기준을 갱신. 이전 연결 응답, 다른 대화·소유자, 온라인 상태의 역행은 계속 거부.
- 노트북 화면에 전달하는 상태 번호는 계속 증가하며, 지시·승인을 자동 재전송하지 않음.


## 0.19.0 — 승인·질문 응답 확장

- 파일 변경, 선택형/직접 입력 질문과 초기 설정 질문, 일반 MCP/URL/스칼라 확장 폼 응답 추가. 원래 GPU 요청·소유자·턴을 재검증하고 중복·불확실 응답의 재전송을 방지.
- 명령의 네트워크 정책 변경 제안과 개별 STDIN 승인 콜백 지원. 요청에 제시하지 않은 정책은 전달하지 않음.
- 원본 계획 실행 버튼을 마지막 완료 계획에 묶어 Plan → Default 모드로 전달. GPU 지침·권한 유지.
- 원격 응답 경로가 없는 옵션 선택기 응답 때문에 앱 연결이 끊기는 문제 수정. 미지원 요청에 연결 창 안내 추가.
- 질문·폼 값은 새 응답 기록에 저장하지 않고 해시만 보관. 인증/설치/파일 폼 등 남은 제한은 승인 지원표에 명시.

## 0.18.3 — 바탕 화면 설치 복구 및 권한·대기열 조회 연결

- MSIX 앱의 하위 프로세스에서 설치하면 AppData가 앱 전용 폴더로 리디렉션되어 바탕 화면 아이콘에서 보이지 않을 수 있는 원인 확인.
- 두 설치 대상 폴더에 고유 임시 파일을 써서 실제 저장 경로를 확인하고, 리디렉션되면 설치 파일·포인터·아이콘 작성 전에 거부하고, Explorer에서 Install.cmd를 실행하도록 안내.
- 프로그램 파일 검증 후에만 검사 모듈을 로드. 설치 복구 시험과 실행 환경 시험을 분리.
- `thread/queue/list` 조회 경로 누락 수정. 작업 ID와 페이지 인수를 검사하고 GPU의 최신 대기열을 읽음. 조회 실패를 빈 대기열로 처리하지 않음.
- 인터넷·파일 시스템 추가 권한 카드의 사용자 응답 전달 지원. 원래 요청의 권한 그룹과 허용 기간을 보존하고, 활성 요청·소유자를 다시 확인한 후 한 번만 전달. 요청 경로는 승인 기록에 저장하지 않음.


## 0.18.2 — Codex 업데이트 호환성

- 노트북 앱 26.908.4834.0과 호스트 앱 26.903.8094.0 조합 지원 추가. 알 수 없는 빌드는 계속 차단.
- 새 모델 설정 요청의 `applied` 응답을 이전 호스트의 확인 응답에서 변환.
- 조건부 요청을 일반 쓰기로 바꾸지 않으며 실행 중 턴의 권한 변경을 모델 선택으로 처리하지 않음.
- 두 모델 설정 규약, 잘못된 응답 및 조건부 요청의 무전송을 회귀 검사.


## 0.18.1 — 설치 복구 및 연결 안정성 개선

- 설치 파일을 임시 폴더에서 복사·검증한 뒤 완성된 버전 폴더로 적용. 중간 실패는 재실행으로 복구하고, 구형 미완성 설치는 검증 후 보존. 정상·변조·사용 중·등록된 설치는 덮어쓰지 않음.
- 설치 포인터를 백업과 함께 원자적으로 교체하고 Windows 사용자별 설치 동시 실행 방지.
- 실제 Windows 사용자 SID에 따라 연결 창 중복 실행 제한을 분리.
- 명령 승인 시각의 비정상 값·미래 값 거부. 실제 요청·선택 대조와 재전송 방지 유지.
- 진단 JSON의 원자적 교체, 읽기 잠금 중 이전 보고서 유지, 정상 종료 시 해당 실행이 만든 연결 파일 정리.
- CLI 클라이언트 식별 버전을 package.json과 일치시킴.
- OneDrive 클라우드 파일 표식을 실제 링크와 구분하고 파일 해시 검증을 유지.
- 비정상 종료 후 PID가 재사용되어도 충돌하지 않는 연결 파일명, 기존/신규 파일 형식의 창 복원 지원.
- 진단 기록을 200ms 단위로 병합하고 상태 전이는 즉시 기록. 종료 시 잠금으로 실패한 최종 기록은 제한적으로 재시도.
- 설치·실행 뮤텍스를 동일 사용자 로그인 세션 간에도 공유하고, 절단 파일의 수동 보존·복구 절차 및 실제 CIM 검사 스크립트 추가.
- 진단 폴더 삭제·디스크 부족이 실행 중 연결을 종료하지 않도록 처리하고, 강제 종료로 남은 런처 잠금 즉시 복구.
- 설치 완료 명세 복사본의 해시까지 적용 전에 검증. 클라우드/실제 링크 태그와 손상된 명세 거부 검사 추가.

## 0.18.0 — 첫 공개 시험판

- 개인 PC 전용 주소·사용자명·설치 경로를 기기별 연결 설정으로 교체.
- 사용자별 Windows 설치, 호스트/클라이언트 역할, 바탕 화면 실행 아이콘, 기존 창 복원.
- SSH 서버 지문 확인, 키/agent 인증, 연결 진단과 호스트 등록.
- CLI 캐시 자동 탐색, 역할별 Codex 호환성 검사, 양쪽 브리지 버전 일치 확인.
- 호스트별 설정·최근 작업·보고서 분리. 첫 작업을 개인 고정 ID 대신 호스트 목록에서 선택.
- Node.js 및 고정 의존성을 포함한 오프라인 ZIP, 체크섬, 업데이트·제거 기능.
- 기존 세션·프로젝트 조회, 본문 사전 읽기, 입력, 새 작업, 모델 및 검증된 승인 전달 유지.
- 개요, 사용설명서, 호환성·문제 해결·승인 지원표 추가.

기존 개인용 버전의 대화, 진단 기록, 계정 데이터와 체크포인트는 공개 저장소에 포함하지 않습니다.
