# 기여 안내

Windows x64와 Node.js 24 이상을 사용합니다. `npm ci --ignore-scripts` 후 `Build-Guard.ps1`, `npm test`, `npm run audit`를 실행하세요. 공개 릴리스는 `scripts/Build-Release.ps1`로 생성합니다.

생성된 ZIP의 압축 해제 폴더를 `scripts/Test-Installation.ps1 -Stage <폴더>`에 전달하면 임시 사용자 폴더에서 설치·재설치·변조 거부·제거를 검사합니다. 프로세스 조회를 제한하는 샌드박스에서는 일반 PowerShell에서 실행해야 할 수 있습니다.

비공개 Codex IPC의 새 버전을 추가할 때는 실제 설치 코드와 요청 형식을 확인하고, 읽기·입력·승인·중복 방지·재연결 동작을 검증해야 합니다. 버전 목록만 바꾸거나 알 수 없는 요청을 모두 통과시키지 않습니다.

테스트에는 합성 작업 ID와 예시 경로만 사용합니다. 자신의 계정·대화·작업 제목·실제 SSH 주소·키를 커밋하지 마세요. 공식 앱 코드와 인증 파일도 배포하지 않습니다.

원격 실제 작업 테스트는 별도의 무해한 테스트 작업에서 수행합니다. 다른 사용자의 실행 중인 작업을 자동 중단하거나 지시를 보내지 않습니다.
