# 보안과 데이터

이 도구는 사용자가 접근 권한을 가진 Windows 호스트에 SSH로 연결합니다. 연결 계정의 SSH 권한은 별도로 관리해야 하며, 브리지 설정이 SSH 계정 자체의 권한을 제한하는 것은 아닙니다.

- 서버 키는 호스트 PC에서 확인한 SHA256 지문과 대조합니다.
- 명령행 전달은 구조화된 SSH 인수와 인코딩된 데이터로 구성합니다.
- 원격 작업 입력과 승인은 정확한 원래 작업 소유자·요청에만 전달합니다.
- 연결이 불확실하면 입력을 자동 재전송하지 않습니다.
- 알려지지 않은 실행/승인 요청과 지원하지 않는 앱 버전은 차단합니다.
- 브리지의 실행 제한 계층은 호스트 공식 Codex의 승인·샌드박스 정책을 대체하지 않습니다.

배포 ZIP에는 키·계정·대화·로컬 보고서가 포함되지 않습니다. 설치 후 생성되는 보고서와 임시 앱 프로필은 개인 데이터입니다. 공개 이슈에 올리지 마세요.

보안 문제를 발견하면 실제 인증 정보나 업무 내용을 게시하지 말고, 민감하지 않은 재현 조건만 공유하세요. 공개 저장소의 GitHub Private vulnerability reporting이 활성화되어 있으면 해당 경로를 이용할 수 있습니다.
